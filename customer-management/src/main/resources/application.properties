# Server Port
server.port=8081

# Application Name
spring.application.name=customer-management

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/nexpos
spring.data.mongodb.database=nexpos

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.nexpos.customer=DEBUG

# Other custom properties (if needed)
customer.service.default-loyalty-points=100

